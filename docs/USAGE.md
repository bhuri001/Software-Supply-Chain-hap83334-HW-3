
---

# 2️⃣ **documentation_interface**

This means:  
> “Document your public interface (CLI, functions, API).”

Since your project is simple, just add a **README section** like:

```markdown
## Command Line Interface (CLI)

Run:

```bash
poetry run python src/my_rekor/main.py
